#! /usr/bin/env node

/**
 * Utility to manage profiles for different utilities.
 * @module Tools/top/Identity
*/

const _ = require( '../include/Top.s' );
module[ 'exports' ] = _;
if( !module.parent )
_.censor.Cui.Exec();
