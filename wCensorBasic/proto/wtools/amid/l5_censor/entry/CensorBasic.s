( function _CensorBasic_s_( )
{

'use strict';

/**
 * Tool to edit files massively and with undo.
  @module Tools/mid/CensorBasic
*/

if( typeof module !== 'undefined' )
{
  const _ = require( '../include/Mid.s' );
  module[ 'exports' ] = _global_.wTools;
}

} )();
