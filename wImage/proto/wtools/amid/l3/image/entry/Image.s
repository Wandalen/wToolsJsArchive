( function _Image_s_( )
{

'use strict';

/**
 * Set of standardized abstract interface to open / read / parse / write / convert / compress / transform / process / filter image with collection of plugins.
  @module Tools/mid/Image
*/

if( typeof module !== 'undefined' )
{
  const _ = require( '../include/Image.s' )
  module[ 'exports' ] = _global_.wTools;
}

})();
